<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:AILanguageLearningApp.PageModels"
             xmlns:models="clr-namespace:AILanguageLearningApp.Models"
             xmlns:local="clr-namespace:AILanguageLearningApp"
             x:DataType="pageModels:MainPageModel"
             x:Class="AILanguageLearningApp.MainPage">

    <ContentPage.Resources>
        <ResourceDictionary>
            <local:InvertedBoolConverter x:Key="InvertedBoolConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Label
                Text="Hello, World!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome to &#10;.NET Multi-platform App UI"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Label Text="Ollama Model Selector" 
                   FontSize="24" 
                   FontAttributes="Bold" 
                   HorizontalOptions="Center" />

            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                               IsVisible="{Binding IsLoading}" 
                               HorizontalOptions="Center" />

            <Border Padding="15" StrokeShape="RoundRectangle 10" StrokeThickness="1">
                <VerticalStackLayout Spacing="15">

                    <Label Text="Heavy Model (Functions &amp; JSON generation):" 
                           FontAttributes="Bold" />
                    <Picker Title="Select Heavy Model"
                            ItemsSource="{Binding AvailableModels}"
                            SelectedItem="{Binding SelectedHeavyModel}" />

                    <BoxView HeightRequest="1" Color="Gray" Margin="0,5" />

                    <Label Text="Light Model (Quick lookups &amp; translations):" 
                           FontAttributes="Bold" />
                    <Picker Title="Select Light Model"
                            ItemsSource="{Binding AvailableModels}"
                            SelectedItem="{Binding SelectedLightModel}" />

                </VerticalStackLayout>
            </Border>

            <Button Text="Refresh Local Ollama Models" 
                    Command="{Binding LoadModelsCommand}"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InvertedBoolConverter}}" 
                    HorizontalOptions="FillAndExpand" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
